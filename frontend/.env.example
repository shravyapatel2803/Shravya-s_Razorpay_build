# ── Frontend Configuration ──────────────────────────────────────────────────
# Target URL for Vite dev proxy (/api -> target) and client API baseURL
VITE_BACKEND_URL=https://shravya-s-razorpay-build.onrender.com

# Optional direct API URL for production if not using reverse proxy:
# VITE_API_URL=https://shravya-s-razorpay-build.onrender.com
